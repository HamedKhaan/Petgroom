# ساخت APK فقط با گوشی و گیت‌هاب

محتوای همین پوشه باید **ریشه مخزن** باشد
(یعنی فایل‌های `app` و `.github` و `settings.gradle.kts` در صفحه اول ریپو دیده شوند).

## ۱) ریپو بساز

1. اپ GitHub را باز کن یا از مرورگر برو به github.com
2. مخزن جدید بساز (Public یا Private فرقی ندارد)
3. فایل‌های این پروژه را در همان مخزن آپلود کن

اگر آپلود پوشه از گوشی سخت است، از لپ‌تاپ یک‌بار پوشه را بفرست؛ بعد از آن ساخت APK همیشه از گوشی انجام می‌شود.

## ۲) Action را اجرا کن

1. داخل ریپو برو به تب **Actions**
2. گردش‌کار **Build APK** را باز کن
3. **Run workflow** را بزن

چند دقیقه طول می‌کشد.

## ۳) APK را روی گوشی بریز

1. وقتی سبز شد، روی همان اجرا بزن
2. بخش **Artifacts** → فایل `petgroom-sms` را دانلود کن
3. زیپ را باز کن؛ فایل `.apk` را نصب کن
4. اجازه نصب از منابع ناشناس را برای مرورگر/گیت‌هاب بده
5. اجازه **پیامک** را داخل اپ تأیید کن

## کار روزمره فروشنده

1. از برنامه کامپیوتر یا همان اکسل، فایل CSV را به گوشی بفرست
2. در اپ «انتخاب فایل مخاطبین» را بزن
3. «شروع ارسال خودکار» را بزن

پیامک از سیم‌کارت خود گوشی می‌رود.
